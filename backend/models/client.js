class Client {
    constructor(userId, username, photoBase64) {
        this.userId = userId;
        this.username = username;
        this.photoBase64 = photoBase64;
    }
}

module.exports = Client;